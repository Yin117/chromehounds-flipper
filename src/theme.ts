import { createTheme } from '@mantine/core';

export const theme = createTheme({
  // components: {
  //   Title: Title.extend({
  //     vars: () => ({
  //       root: {
  //         // This specifically kills the --title-mt variable Mantine uses
  //         '--title-mt': '0',
  //         '--title-mb': '0', // Optional: removes bottom margin too
  //       },
  //     }),
  //   }),
  // },
});
